package com.wm38.course.services;

import java.util.List;
import java.util.Optional;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.wm38.course.entities.Category;
import com.wm38.course.repositories.CategoryRepository;

@Service
public class CategorySerice {

	//Informa que o CategoryService depende do CategoryRepository
	@Autowired
	private CategoryRepository repository;
	
	public List<Category> findAll(){
		return repository.findAll();
	}
	
	public Category findById(Long id) {
		Optional<Category> obj = repository.findById(id);
		return obj.get();
	}
	
}
